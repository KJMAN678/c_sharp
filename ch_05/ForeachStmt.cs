using System;

class MainClass
{
    static void Main()
    {
        string[] j = { "グー", "チョキ", "パー" };

        foreach (string str in j)
        {
            Console.WriteLine(str);
        }
    }
}